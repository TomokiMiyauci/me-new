query Blog($lang: String!) {
  allBlog(where: { language: { eq: $lang } }) {
    title
    description
    coverImage {
      ...Picture
    }
  }
}

query HomeByLang($lang: String!) {
  home: allHome(where: { language: { eq: $lang } }) {
    ...HomeMeta
  }
}
