query PostBySlug($slug: String!) {
  allPost(where: { slug: { current: { eq: $slug } } }) {
    ...Post_post_meta
    id: _id
    title
    bodyRaw
    description
    categories {
      name
    }
    tags {
      name
    }
    _createdAt
    _updatedAt
    createdAt
    updatedAt
  }
}
