query PostBySlug($slug: String!) {
  allPost(where: { slug: { current: { eq: $slug } } }) {
    id: _id
    title
    bodyRaw
    description
    categories {
      name
    }
    tags {
      name
    }
    _createdAt
    _updatedAt
    createdAt
    updatedAt
  }
}
