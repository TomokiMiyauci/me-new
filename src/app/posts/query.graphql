query GetAllPost($lang: String!) {
  allPost(where: { language: { eq: $lang } }) {
    ...Article_article
    key: _id
    slug {
      current
    }
  }
}

query GetBlog($lang: String!) {
  allBlog(where: { language: { eq: $lang } }) {
    title
    description
  }
}
