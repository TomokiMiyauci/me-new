import type { JSX } from "react";

export default function NotFound(): JSX.Element {
  return (
    <html>
      <body>
        <main>
          <h1>Not Found</h1>
        </main>
      </body>
    </html>
  );
}
